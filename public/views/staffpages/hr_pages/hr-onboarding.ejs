<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>HR Onboarding</title>
    <link rel="stylesheet" href="/css/sidebar.css"> 

    <!-- Import Inter font -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">

    <style>
        body {
            font-family: 'Inter', sans-serif;
            background-color: #f9f9f9;
            color: #333;
            margin: 0;
            padding: 0;
            display: flex;
        }

        /* Main content area */
        .main-content {
            margin-left: 250px;
            width: calc(100% - 250px);
            padding: 20px;
            overflow-x: hidden;
            transition: margin-left 0.3s ease;
        }

        /* Header styles */
        .header-container {
            background-color: #fff;
            padding: 20px 0;
            box-shadow: 0px 2px 5px rgba(0, 0, 0, 0.1);
            margin-bottom: 20px;
        }

        .header {
            display: flex;
            align-items: center;
            justify-content: center;
            margin-top: 10px;
            margin-bottom: 10px;
        }

        .header .logo {
            position: absolute;
            left: 3%;
            width: 200px;
        }

        .header h1 {
            font-size: 2em;
            color: #07ACB9;
            margin: 0;
        }

        /* Subheader styles */
        .subheader {
            font-size: 1.5em;
            color: #333;
            font-weight: bold;
            text-align: center;
            margin-top: 50px;
            margin-bottom: 60px;
        }

        /* Checklist guide styles */
        .table-header-left {
            font-size: 1.2em;
            color: #333;
            font-weight: bold;
            margin-bottom: 10px;
            margin-left: 10%;
        }

        /* Table styles */
        table {
            width: 80%;
            margin: 0 auto;
            border-collapse: collapse;
            margin-bottom: 20px;
        }

        table th, table td {
            padding: 12px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }

        table th {
            background-color: #f2f2f2;
            color: #333;
            font-weight: bold;
        }

        table input[type="text"] {
            width: 100%;
            padding: 8px;
            border: 1px solid #ddd;
            border-radius: 4px;
            font-size: 1em;
            box-sizing: border-box;
        }

        /* Button styles */
        .btn-add, .btn-edit, .btn-delete, .btn-save {
            padding: 5px 10px;
            background-color: #07ACB9;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            margin-right: 10px;
        }

        .btn-add {
            background-color: #124A5C;
            margin: 20px 0;
        }

        .btn-add:hover {
            background-color: #059ca4;
        }

        .btn-edit:hover, .btn-save:hover, .btn-delete:hover {
            background-color: #059ca4;
        }

        .btn-delete {
            background-color: #D9534F;
        }

        /* Add item section */
        .add-item {
            text-align: center;
        }
        /* Notes section */
        .notes {
            width: 80%;
            margin: 20px auto;
        }

        .notes label {
            display: block;
            font-weight: bold;
            margin-bottom: 7px;
        }

        /* Notes section */
        textarea {
            width: 100%;
            padding: 10px;
            border: 1px solid #ddd;
            border-radius: 4px;
            resize: vertical;
        }

        /* Submit button */
        .btn-submit {
            display: block;
            padding: 15px 24px;
            background-color: #124A5C;
            color: #fff;
            border: none;
            border-radius: 10px;
            font-size: 15px;
            cursor: pointer;
            text-align: center;
            margin: 30px auto;
            transition: background-color 0.3s;
        }

        .btn-submit:hover {
            background-color: #059ca4;
        }

    </style>
</head>
<body>
    <%- include('../../partials/hr_partials') %>
    <!-- Main content -->
    <div class="main-content">
        <div class="header-container">
            <div class="header">
                <img src="/images/prime_assets/primeinfra-logo-with-name.png" alt="Prime Infra Logo" class="logo">
                <h1>Onboarding</h1>
            </div>
        </div>

        <div class="subheader">1. Accomplishing of First Day Checklist</div>

        <div class="table-header-left">First Day Checklist Guide (HR View)</div>

        <!-- Editable Table with checklist items -->
        <table id="checklist-table">
            <thead>
                <tr>
                    <th>Activities</th>
                    <th>Where</th>
                    <th>Contact Person</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td><input type="text" value="Finding your Workstation + Meeting your Team"></td>
                    <td><input type="text" value="20th Floor, Facilities"></td>
                    <td><input type="text" value="Kyrah Santos"></td>
                    <td><button class="btn-edit">Edit</button> <button class="btn-delete">Delete</button></td>
                </tr>
                <tr>
                    <td><input type="text" value="Creation of company ID"></td>
                    <td><input type="text" value="HR Department"></td>
                    <td><input type="text" value="Lia Bacani"></td>
                    <td><button class="btn-edit">Edit</button> <button class="btn-delete">Delete</button></td>
                </tr>
                <tr>
                    <td><input type="text" value="Building/Door access"></td>
                    <td><input type="text" value="Security Office"></td>
                    <td><input type="text" value="Paul Bulaon"></td>
                    <td><button class="btn-edit">Edit</button> <button class="btn-delete">Delete</button></td>
                </tr>
            </tbody>
        </table>

        <div class="add-item">
            <button class="btn-add">Add New Item</button>
        </div>

        <div class="notes">
            <label for="notes">Notes:</label>
            <textarea id="notes" placeholder="Add notes here..."></textarea>
        </div>

        <button class="btn-submit">Save Changes</button>
    </div>

    <script>
        document.querySelector('.btn-add').addEventListener('click', function() {
            const table = document.querySelector('#checklist-table tbody');
            const newRow = document.createElement('tr');
            newRow.innerHTML = `
                <td><input type="text" value="New Item"></td>
                <td><input type="text" value="Location"></td>
                <td><input type="text" value="Contact Person"></td>
                <td><button class="btn-edit">Edit</button> <button class="btn-delete">Delete</button></td>
            `;
            table.appendChild(newRow);
        });

        // Handle Edit and Delete buttons
        document.querySelector('#checklist-table').addEventListener('click', function(event) {
            if (event.target.classList.contains('btn-delete')) {
                event.target.closest('tr').remove();
            }
            if (event.target.classList.contains('btn-edit')) {
                const row = event.target.closest('tr');
                const inputs = row.querySelectorAll('input');
                inputs.forEach(input => input.removeAttribute('readonly'));
                event.target.textContent = 'Save';
                event.target.classList.replace('btn-edit', 'btn-save');
            }
            if (event.target.classList.contains('btn-save')) {
                const row = event.target.closest('tr');
                const inputs = row.querySelectorAll('input');
                inputs.forEach(input => input.setAttribute('readonly', true));
                event.target.textContent = 'Edit';
                event.target.classList.replace('btn-save', 'btn-edit');
            }
        });

        // Toggle active class for sidebar links
        document.querySelectorAll('.sidebar ul li a').forEach(link => {
            link.addEventListener('click', function() {
                document.querySelectorAll('.sidebar ul li a').forEach(link => link.classList.remove('active'));
                this.classList.add('active');
            });
        });

        // Toggle collapsible content
        document.querySelectorAll('.collapsible-header').forEach(header => {
            header.addEventListener('click', function() {
                var content = this.nextElementSibling;
                if (content.style.display === 'block') {
                    content.style.display = 'none';
                } else {
                    // Hide all other collapsible contents
                    document.querySelectorAll('.collapsible-content').forEach(item => {
                        if (item !== content) {
                            item.style.display = 'none';
                        }
                    });
                    content.style.display = 'block';
                }
            });
        });
    </script>

</body>
</html>
