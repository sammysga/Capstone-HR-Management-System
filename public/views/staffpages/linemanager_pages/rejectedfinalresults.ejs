<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Final Results - Rejected</title>
    <link rel="stylesheet" href="/css/sidebar.css"> <!-- Linking Sidebar CSS -->
    <link rel="stylesheet" href="/css/linemanager.css"> <!-- Linking Main Content CSS -->
    <style>
        .main-content {
            margin-left: 350px;
            padding: 20px;
            width: calc(100% - 350px);
        }
        .rejection-message {
            padding: 20px;
            background-color: #f8d7da;
            color: #721c24;
            border: 1px solid #f5c6cb;
            margin-bottom: 20px;
        }
        .rejection-message h2 {
            font-size: 24px;
        }
        .rejection-details {
            margin-top: 20px;
        }
        .rejection-details p {
            font-size: 18px;
        }
        .back-button {
            padding: 10px 20px;
            background-color: #007BFF;
            color: white;
            border: none;
            cursor: pointer;
            font-size: 16px;
            margin-top: 20px;
        }
        .back-button:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <%- include('../../partials/linemanager_partials') %>

    <!-- Main Content Section -->
    <div class="main-content">
        <h2>Final Results - Rejected</h2>

        <!-- Rejection Message -->
        <div class="rejection-message">
            <h2>We regret to inform you that the applicant has been rejected.</h2>
            <p>The candidate did not meet the required standards for the position.</p>
        </div>

        <!-- Applicant Details -->
        <div class="rejection-details">
            <h3>Applicant Details</h3>
            <p><strong>Name:</strong> <%= name %></p>
            <p><strong>Position Applied:</strong> <%= position %></p>
            <p><strong>Department:</strong> <%= department %></p>

            <h3>Summary of Results:</h3>
            <ul>
                <li><strong>Initial Screening Interview Result:</strong> <%= screeningResult %></li>
                <li><strong>HR Evaluation:</strong> <%= hrevaluation %></li>
                <li><strong>Line Manager Evaluation:</strong> <%= lineManagerEvaluation %></li>
            </ul>

            <h3>Reason for Rejection:</h3>
            <p><%= rejectionReason %></p>
        </div>

        <!-- Back Button -->
        <button class="back-button" onclick="window.history.back()">Back</button>
    </div>
</body>

<script>

    // Toggle active class for sidebar links
    document.querySelectorAll('.sidebar ul li a').forEach(link => {
        link.addEventListener('click', function() {
            document.querySelectorAll('.sidebar ul li a').forEach(link => link.classList.remove('active'));
            this.classList.add('active');
        });
    });

    // Toggle collapsible content
    document.querySelectorAll('.collapsible-header').forEach(header => {
        header.addEventListener('click', function() {
            var content = this.nextElementSibling;
            if (content.style.display === 'block') {
                content.style.display = 'none';
            } else {
                // Hide all other collapsible contents
                document.querySelectorAll('.collapsible-content').forEach(item => {
                    if (item !== content) {
                        item.style.display = 'none';
                    }
                });
                content.style.display = 'block';
            }
        });
    });
</script>

</html>
