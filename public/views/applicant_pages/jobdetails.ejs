<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= job.jobRole %> - Job Details</title>
    <link rel="stylesheet" href="/css/publicapplicant.css">
    <style>
        .job-details {
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
        }

        .job-details h1 {
            font-size: 2em;
            margin-bottom: 20px;
            color: #333;
        }

        .job-details .description {
            margin-bottom: 20px;
            color: #666;
        }

        .apply-button {
            display: inline-block;
            padding: 10px 20px;
            background-color: #4CAF50;
            color: white;
            text-align: center;
            border-radius: 5px;
            cursor: pointer;
            text-decoration: none;
        }

        .apply-button:hover {
            background-color: #45a049;
        }
    </style>
</head>
<body>
    <%- include('../partials/publicheader') %>

    <div class="job-details">
        <h1><%= job.jobRole %></h1>
        <p><strong>Department:</strong> <%= job.department %></p>
        <p class="description"><%= job.description %></p>
        <p><strong>Status:</strong> <%= job.status === "Active" ? "Active" : "Inactive" %></p>

        <a href="/apply/<%= job.jobOfferUUID %>" class="apply-button">Apply Now</a>
    </div>
</body>
</html>
