<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Contact Us - Prime Infra</title>
    <link rel="stylesheet" href="/css/publicapplicant.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        :root {
            --primary: #07ACB9;
            --primary-dark: #056770;
            --primary-light: rgba(7, 172, 185, 0.1);
            --text-dark: #1e293b;
            --text-medium: #475569;
            --text-light: #64748b;
            --border-color: #e2e8f0;
            --input-border: #cbd5e1;
            --error: #ef4444;
            --success: #10b981;
            --white: #ffffff;
            --bg-light: #f8fafc;
            --shadow: 0 6px 24px rgba(0, 0, 0, 0.08);
        }

        body {
            font-family: 'Poppins', sans-serif;
            background-color: var(--bg-light);
            margin: 0;
            padding: 0;
            font-size: 16px;
            color: var(--text-medium);
            line-height: 1.6;
        }

        .page-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 40px 20px;
        }

        .page-title {
            text-align: center;
            margin-bottom: 30px;
            color: var(--text-dark);
            font-size: 32px;
            font-weight: 600;
        }

        .page-description {
            text-align: center;
            max-width: 700px;
            margin: 0 auto 40px;
            color: var(--text-light);
            font-size: 16px;
            line-height: 1.7;
        }

        .contact-container {
            display: flex;
            flex-direction: column;
            align-items: center;
            margin-bottom: 40px;
        }

        .contact-form {
            width: 100%;
            max-width: 650px;
            padding: 40px;
            background-color: var(--white);
            border-radius: 12px;
            box-shadow: var(--shadow);
            position: relative;
        }

        .form-title {
            text-align: center;
            margin-bottom: 30px;
            color: var(--primary);
            font-size: 24px;
            font-weight: 600;
            position: relative;
            padding-bottom: 15px;
        }

        .form-title:after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 50%;
            transform: translateX(-50%);
            height: 3px;
            width: 60px;
            background-color: var(--primary);
            border-radius: 3px;
        }

        .form-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
            width: 100%;
        }

        .form-grid .full-width {
            grid-column: 1 / -1;
        }
        
        @media (max-width: 768px) {
            .form-grid {
                grid-template-columns: 1fr;
                gap: 15px;
            }
        }

        .form-group {
            margin-bottom: 25px;
        }

        .form-group label {
            display: block;
            margin-bottom: 8px;
            font-weight: 500;
            color: var(--text-medium);
            font-size: 15px;
        }

        .form-control {
            width: 100%;
            padding: 12px 16px;
            font-size: 15px;
            border: 1px solid var(--input-border);
            border-radius: 8px;
            transition: all 0.3s ease;
            background-color: #fafafa;
            color: var(--text-dark);
            font-family: 'Poppins', sans-serif;
            box-sizing: border-box;
        }

        .form-control:focus {
            border-color: var(--primary);
            box-shadow: 0 0 0 3px rgba(7, 172, 185, 0.2);
            outline: none;
            background-color: var(--white);
        }

        textarea.form-control {
            height: 150px;
            resize: vertical;
        }

        .form-group select.form-control {
            appearance: none;
            background-image: url('data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="%2364748b" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="6 9 12 15 18 9"></polyline></svg>');
            background-repeat: no-repeat;
            background-position: right 12px center;
            background-size: 16px;
            padding-right: 40px;
        }

        .submit-button {
            display: inline-block;
            width: 100%;
            padding: 14px 24px;
            background: linear-gradient(135deg, var(--primary), var(--primary-dark));
            color: white;
            border: none;
            border-radius: 8px;
            font-size: 16px;
            font-weight: 500;
            cursor: pointer;
            transition: all 0.3s ease;
            text-align: center;
            box-shadow: 0 4px 6px rgba(7, 172, 185, 0.2);
        }

        .submit-button:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 12px rgba(7, 172, 185, 0.25);
        }

        .submit-button:active {
            transform: translateY(0);
            box-shadow: 0 2px 4px rgba(7, 172, 185, 0.2);
        }

        .form-error {
            color: var(--error);
            font-size: 13px;
            margin-top: 6px;
            font-weight: 500;
            display: flex;
            align-items: center;
        }

        .form-error:before {
            content: '\f071';
            font-family: 'Font Awesome 6 Free';
            font-weight: 900;
            margin-right: 6px;
            font-size: 12px;
        }

        .contact-info {
            display: flex;
            justify-content: center;
            flex-wrap: wrap;
            gap: 30px;
            margin-top: 40px;
        }

        .info-item {
            display: flex;
            align-items: center;
            margin-bottom: 20px;
        }

        .info-icon {
            width: 40px;
            height: 40px;
            background-color: var(--primary-light);
            color: var(--primary);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-right: 15px;
            flex-shrink: 0;
        }

        .info-text {
            color: var(--text-medium);
        }

        .info-text strong {
            display: block;
            color: var(--text-dark);
            margin-bottom: 3px;
        }

        .required-field::after {
            content: '*';
            color: var(--error);
            margin-left: 4px;
        }

        .form-note {
            font-size: 13px;
            color: var(--text-light);
            text-align: center;
            margin-top: 20px;
        }

        @media (max-width: 768px) {
            .contact-form {
                padding: 30px 25px;
            }

            .page-title {
                font-size: 26px;
            }
        }
    </style>
</head>
<body>
    <%- include('../partials/publicheader') %>
    
    <div class="page-container">
        <h1 class="page-title">Get in Touch</h1>
        <p class="page-description">We'd love to hear from you. Please fill out the form below and we'll get back to you as soon as possible.</p>
        
        <div class="contact-container">
            <section class="contact-form">
                <h2 class="form-title">Contact Us</h2>
                
                <form action="/contact" method="POST">
                    <div class="form-grid">
                        <div class="form-group">
                            <label for="firstName" class="required-field">First Name</label>
                            <input type="text" class="form-control" name="firstName" id="firstName" required>
                            <% if (errors.firstName) { %><p class="form-error"><%= errors.firstName %></p><% } %>
                        </div>
                        
                        <div class="form-group">
                            <label for="lastName" class="required-field">Last Name</label>
                            <input type="text" class="form-control" name="lastName" id="lastName" required>
                            <% if (errors.lastName) { %><p class="form-error"><%= errors.lastName %></p><% } %>
                        </div>
                        
                        <div class="form-group full-width">
                            <label for="email" class="required-field">Email Address</label>
                            <input type="email" class="form-control" name="email" id="email" required>
                            <% if (errors.email) { %><p class="form-error"><%= errors.email %></p><% } %>
                        </div>
                        
                        <div class="form-group full-width">
                            <label for="inquiry" class="required-field">Inquiry Type</label>
                            <select class="form-control" name="inquiry" id="inquiry" required>
                                <option value="">Please select...</option>
                                <option value="general">General Inquiry</option>
                                <option value="support">Technical Support</option>
                                <option value="feedback">Feedback</option>
                                <option value="business">Business Opportunity</option>
                            </select>
                            <% if (errors.inquiry) { %><p class="form-error"><%= errors.inquiry %></p><% } %>
                        </div>
                        
                        <div class="form-group full-width">
                            <label for="subject" class="required-field">Subject</label>
                            <input type="text" class="form-control" name="subject" id="subject" required>
                            <% if (errors.subject) { %><p class="form-error"><%= errors.subject %></p><% } %>
                        </div>
                        
                        <div class="form-group full-width">
                            <label for="message" class="required-field">Message</label>
                            <textarea class="form-control" name="message" id="message" required></textarea>
                            <% if (errors.message) { %><p class="form-error"><%= errors.message %></p><% } %>
                        </div>
                        
                        <div class="form-group full-width">
                            <button type="submit" class="submit-button">
                                <i class="fas fa-paper-plane"></i> Send Message
                            </button>
                        </div>
                    </div>
                </form>
                
                <p class="form-note">Fields marked with an asterisk (*) are required.</p>
            </section>
            
            <div class="contact-info">
                <div class="info-item">
                    <div class="info-icon">
                        <i class="fas fa-map-marker-alt"></i>
                    </div>
                    <div class="info-text">
                        <strong>Address</strong>
                        Company ABC Capital, Inc., Three E-Com Center, Pasay, Manila
                    </div>
                </div>
                
                <div class="info-item">
                    <div class="info-icon">
                        <i class="fas fa-envelope"></i>
                    </div>
                    <div class="info-text">
                        <strong>Email</strong>
                        info@primeinfra.com
                    </div>
                </div>
                
                <div class="info-item">
                    <div class="info-icon">
                        <i class="fas fa-phone-alt"></i>
                    </div>
                    <div class="info-text">
                        <strong>Phone</strong>
                        +63 (2) 8888-8888
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>