<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Contact Us - Prime Infra</title>
    <link rel="stylesheet" href="/css/publicapplicant.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background-color: #f4f4f4;
            margin: 0; /* Remove default margin */
            padding: 0; /* Remove padding to prevent spacing issues */
            font-size: 16px; /* Set standard base font size */
        }

        header {
            margin: 0; /* Reset header margin */
            padding: 0; /* Reset header padding */
            /* Add any other header-specific styles here */
        }

        .contact-form {
            max-width: 600px;
            margin: 40px auto;
            padding: 40px; /* Increased padding for spaciousness */
            background-color: #ffffff; /* White background for the form */
            border-radius: 10px; /* More rounded corners */
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1); /* More pronounced shadow */
            border: 1px solid #ddd; /* Subtle border for definition */
        }

        .contact-form h2 {
            text-align: center;
            margin-bottom: 20px;
            color: #333;
            font-size: 24px; /* Standard size for headings */
        }

        .form-group {
            margin-bottom: 20px;
        }

        .form-group label {
            margin-bottom: 8px;
            font-weight: 600;
            color: #555;
            display: block;
            font-size: 16px; /* Standard size for labels */
        }

        .form-group input,
        .form-group textarea,
        .form-group select {
            width: 100%;
            padding: 12px;
            font-size: 16px; /* Standard size for input elements */
            border: 1px solid #ccc;
            border-radius: 4px;
            transition: border-color 0.3s, box-shadow 0.3s;
        }

        .form-group input:focus,
        .form-group select:focus,
        .form-group textarea:focus {
            border-color: #4CAF50;
            box-shadow: 0 0 5px rgba(76, 175, 80, 0.5);
            outline: none;
        }

        .form-group textarea {
            height: 150px;
            resize: vertical;
        }

        .form-group button {
            padding: 12px;
            background-color: #4CAF50;
            color: white;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 16px; /* Standard size for buttons */
            transition: background-color 0.3s;
            width: 100%;
        }

        .form-group button:hover {
            background-color: #45a049;
        }

        .form-error {
            color: red;
            font-size: 14px; /* Slightly smaller for error messages */
            margin-top: 5px;
            font-weight: 600; /* Make error message bold */
        }

        @media (max-width: 600px) {
            .contact-form {
                padding: 20px; /* Reduce padding on smaller screens */
            }
        }
    </style>
</head>
<body>
    <%- include('../partials/publicheader') %>

    <section class="contact-form">
        <h2>Contact Us</h2>
        <form action="/contact" method="POST">
            <div class="form-group">
                <label for="inquiry">Inquiry</label>
                <select name="inquiry" id="inquiry" required>
                    <option value="">Select Inquiry</option>
                    <option value="general">General Inquiry</option>
                    <option value="support">Support</option>
                    <option value="feedback">Feedback</option>
                </select>
                <% if (errors.inquiry) { %><p class="form-error"><%= errors.inquiry %></p><% } %>
            </div>

            <div class="form-group">
                <label for="lastName">Last Name</label>
                <input type="text" name="lastName" id="lastName" required>
                <% if (errors.lastName) { %><p class="form-error"><%= errors.lastName %></p><% } %>
            </div>

            <div class="form-group">
                <label for="firstName">First Name</label>
                <input type="text" name="firstName" id="firstName" required>
                <% if (errors.firstName) { %><p class="form-error"><%= errors.firstName %></p><% } %>
            </div>

            <div class="form-group">
                <label for="email">Email Address</label>
                <input type="email" name="email" id="email" required>
                <% if (errors.email) { %><p class="form-error"><%= errors.email %></p><% } %>
            </div>

            <div class="form-group">
                <label for="subject">Subject</label>
                <input type="text" name="subject" id="subject" required>
                <% if (errors.subject) { %><p class="form-error"><%= errors.subject %></p><% } %>
            </div>

            <div class="form-group">
                <label for="message">Message</label>
                <textarea name="message" id="message" required></textarea>
                <% if (errors.message) { %><p class="form-error"><%= errors.message %></p><% } %>
            </div>

            <div class="form-group">
                <button type="submit">Submit</button>
            </div>
        </form>
    </section>
</body>
</html>
